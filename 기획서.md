# 비자 행정사용 RAG AI 챗봇 — 모바일 버전 기획서

데스크탑판(Immigration-AI)의 모바일 파생. **클라우드 LLM 전용** (로컬 LLM 제거).

## 1. 개요

행정사가 이동 중에도 스마트폰으로 비자·체류 상담 자료를 질문. 데스크탑판과 동일한 RAG 챗봇이나, **로컬 LLM(Ollama) 없이 클라우드 LLM만** 사용.

- 예시: "D-2 소지자가 취업비자 전환 시 필요 서류?" → 업로드 문서(RAG) 근거로 답변 + 출처.

## 2. 핵심 원칙 (데스크탑과 다른 점)

1. **클라우드 전용** — 로컬 LLM·Ollama 없음. 생성·임베딩 모두 클라우드(OpenAI).
2. **유출가능 문서만** — 모바일은 전송이 전제라 **유출불가(confidential) 문서 업로드 자체 금지.** 태그 분리·2컬렉션 불필요(단일).
3. **오프라인 불가** — 네트워크 필수(클라우드 호출).
4. 문서·벡터·키는 **기기 로컬** 저장. 원격 서버 없음.

## 3. 화면(탭) 구성

- **문서 관리** — PDF 업로드(유출가능 전용), 목록/삭제, 인덱싱 진행률.
- **채팅** — 클라우드 LLM RAG. 세션 히스토리(ChatGPT식).
- **설정** — 공급자(OpenAI/Anthropic) 선택, API 키 입력, 모델 선택.

## 4. 데이터 흐름

```
업로드(유출가능만) → 파싱 → 청킹 → 클라우드 임베딩(OpenAI) → 로컬 벡터DB
질의 → 클라우드 임베딩 → 하이브리드 검색(벡터+키워드) → 컨텍스트+출처 → 클라우드 LLM → 답변
```

## 5. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 앱 | **Tauri v2 모바일** (iOS/Android) | 데스크탑 Rust/React 코드 최대 재사용 |
| 프론트 | React + TS | 모바일 UI 재설계(터치·좁은 화면) |
| 코어 | Rust | 파싱·청킹·검색 재사용 |
| LLM | **OpenAI API** (chat+embedding) | Anthropic(chat)은 선택, 임베딩은 OpenAI |
| 임베딩 | OpenAI `text-embedding-3-small`(1536d) | 클라우드 |
| 벡터DB | 기기 로컬(JSON/SQLite) | 소규모 |
| PDF 파싱 | pdf-extract (Rust) | 데스크탑 검증됨 |
| API 키 | 모바일 보안저장(iOS Keychain/Android Keystore) | 평문 금지 |

## 6. 데스크탑에서 재사용 / 신규

- **재사용**: `documents`(parser·chunker), `rag`(retriever·하이브리드검색·프롬프트), ChatPane UI 패턴.
- **제거**: `llm/ollama`, 로컬 임베딩, 2컬렉션(all/leakable) 분리, Ollama 관리 탭.
- **변경**: 임베딩 로컬→클라우드, 유출 태깅→유출가능 전용, 데스크탑 파일다이얼로그→모바일 문서 피커.
- **신규**: 모바일 보안저장, 반응형 모바일 UI, 앱 스토어 배포.

## 7. 미확정 / 리스크

- **모바일 보안저장**: `keyring` 크레이트 모바일 미지원 가능 → Tauri stronghold 또는 플랫폼 플러그인 검토.
- **Tauri 모바일 성숙도**: iOS/Android 빌드·서명·스토어 심사.
- **pdf-extract 모바일 컴파일**: aarch64 타깃 검증 필요.
- **브라우저 로그인(ChatGPT Plus 연동)**: **비권장/유예.** Plus는 API 접근권 아님 → 약관 위반·계정정지·불안정. API 키가 정식 경로.
- **비용**: 클라우드 임베딩·생성 사용량 과금(사용자 API 키 부담).

## 8. 개발 단계

1. 프로젝트 스캐폴드(Tauri 모바일) + 문서관리 + 파싱/청킹
2. 클라우드 임베딩 + 로컬 벡터DB + 하이브리드 검색
3. 채팅(클라우드 RAG) + 세션 UI
4. 설정(키/모델) + 모바일 보안저장
5. iOS/Android 빌드·서명·배포
